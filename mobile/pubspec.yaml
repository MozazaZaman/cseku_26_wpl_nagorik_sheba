name: nagorik_sheba
description: Nagorik Sheba - Agentic municipal complaint management platform (mobile client)
publish_to: "none"
version: 1.1.0+2

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  geolocator: ^13.0.1
  image_picker: ^1.1.2
  speech_to_text: ^7.0.0
  camera: ^0.11.0+2
  permission_handler: ^11.3.1
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
